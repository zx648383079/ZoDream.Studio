<Window x:Class="ZoDream.Studio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZoDream.Studio"
        xmlns:ctl="clr-namespace:ZoDream.Studio.Controls"
        xmlns:sctl="clr-namespace:ZoDream.Shared.Controls;assembly=ZoDream.Shared.Controls"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Unloaded="Window_Unloaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <sctl:IconButton x:Name="PlayBtn" Icon="&#xE102;" Label="回放"/>
            <sctl:IconButton x:Name="StopBtn" Icon="&#xE103;" Label="停止"/>
            <sctl:IconButton x:Name="OptionBtn" Icon="&#xE115;" Label="设置"/>
        </StackPanel>
        <ctl:PianoPanel x:Name="PianoTb" Background="#fff" OnPress="PianoTb_OnPress" OnRelease="PianoTb_OnRelease"/>
        <ctl:TrackPanel Grid.Row="1"/>
    </Grid>
</Window>
